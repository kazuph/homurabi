{
  "name": "homurabi",
  "version": "0.0.0-phase0",
  "private": true,
  "type": "module",
  "description": "Real Ruby + Sinatra on Cloudflare Workers via Opal",
  "scripts": {
    "build:ruby": "bundle exec opal -c -E --esm --no-source-map app/hello.rb > build/hello.no-exit.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.99.0"
  }
}
