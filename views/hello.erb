<h1>Hello <%= @name %>!</h1>

<p>This page was rendered by Sinatra's <code>erb :hello</code> call,
   which precompiled the template at build time so Opal-on-Workers
   never has to run an <code>eval</code>.</p>

<p><a href="/">&larr; back</a></p>
